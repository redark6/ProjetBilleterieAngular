import { UserCurrentRightDesc } from './user-current-right-desc';

describe('UserCurrentRightDesc', () => {
  it('should create an instance', () => {
    expect(new UserCurrentRightDesc()).toBeTruthy();
  });
});
